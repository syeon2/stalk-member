spring:
  profiles:
    active: prod

---
spring:
  config:
    activate:
      on-profile: test

  data:
    redis:
      host: localhost
      port: 6379
      authentication-code:
        expire: 5

---
spring:
  config:
    activate:
      on-profile: dev

  data:
    redis:
      host: localhost
      port: 6379
      authentication-code:
        expire: 5

