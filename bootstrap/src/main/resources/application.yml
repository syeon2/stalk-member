server:
  port: 0

spring:
  application:
    name: member-service

  profiles:
    group:
      test: test
      dev: dev
      prod: prod
    active: prod

  config:
    import:
      - msa-core.yml
      - monitor.yml
